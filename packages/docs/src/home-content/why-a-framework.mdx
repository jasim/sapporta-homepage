import EvidenceTranscript from "../components/EvidenceTranscript.astro";

## Why a framework instead of more prompting

Agents produce screens quickly, but they do not build the shared parts
underneath: one grid that filters and sorts, APIs that secure their own rows,
forms that follow the schema.

**Ask for a filter, get a one-off.** Ask for a filter on a list of tickets
and the agent writes conditions for the fields you named. It handles equals
but not contains, and breaks when two conditions meet. Fixing it takes rounds
of specifying, checking, correcting, and waiting. Someone still has to build
the UI that shows the filter is active, and clears it.

<EvidenceTranscript
  label="The prompting loop"
  lines={[
    { kind: "user", text: "add filtering to tickets" },
    { kind: "agent", text: "\"contains\" doesn't work" },
    { kind: "agent", text: "breaks with two conditions" },
    { kind: "agent", text: "nothing shows a filter is active" },
    { kind: "user", text: "now the same for invoices…" },
  ]}
/>

**Prompting never generalizes.** Then you want the same on invoices, and the
work repeats. The agent will not stop and treat filtering as a general
feature; that only happens in a system designed around it. Asking feature by
feature never produces a shared architecture — each generated screen filters
a little differently, filtering exists only on the tables you happened to ask
about, and rows are checked only where someone remembered to ask.

**Sapporta provides those shared parts,** declared once and used by every
table, so your attention goes back to your own work — the workflows of your
clinic CRM, your inspection tracker, your vertical SaaS — instead of another
round of describing and fixing plumbing.
